require("omp"):setup({config='~/.config/ooodnakov/local/ohmyposh/gruvbox.omp.json'})
require("full-border"):setup()
require("git"):setup {
	-- Order of status signs showing in the linemode
	order = 1500,
}
require("folder-rules"):setup()