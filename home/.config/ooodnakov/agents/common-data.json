{
  "mcp_servers": [
    { "name": "filesystem", "description": "local repository and home config trees" },
    { "name": "git", "description": "commit history, diff summaries, and branch status" },
    { "name": "shell", "description": "deterministic local inspection commands (rg, fd, git, python3)" }
  ],
  "skills": [
    "Dotfiles portability review (Linux + Windows + macOS safety)",
    "Shell bootstrap audit (idempotency and dry-run behavior)",
    "Secrets hygiene review (template references vs local plaintext files)"
  ]
}
