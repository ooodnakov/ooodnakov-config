# Local-only PowerShell variables.
# Copy to $HOME/.config/ooodnakov/local/env.ps1 and edit there.
#
# Alternatively, use the LOCAL OVERRIDES section inside the synced
# env.ps1 — lines between the START/END markers survive
# `oooconf secrets sync`.

# $env:GITHUB_TOKEN = "replace-me"
# $env:WORK_MACHINE_NAME = "replace-me"
