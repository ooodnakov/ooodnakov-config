local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  position = "top",
  height = 33,
  color = colors.TRANSPARENT,
  padding_left=13,
  padding_right=10,
  y_offset=-2
})
